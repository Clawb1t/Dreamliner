import type { ConfigOverride } from "../../core/types.js";

export const animeDefaultOverrides: ConfigOverride[] = [
  {
    level: ">=0",
    config: {
      can_neko: true,
      can_saved: true,
    },
  },
];
