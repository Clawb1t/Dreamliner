/** Button customId prefixes shared across commands.ts and buttons.ts, kept in their own module
 *  so the two can reference each other's prefixes without an import cycle. */
export const PLANE_STATS_PREFIX = "dl:planestats:";
export const PLANE_PACK_PREFIX = "dl:planepack:";
export const PLANE_LABEL_PREFIX = "dl:planelabel:";
export const PLANE_INVENTORY_PREFIX = "dl:planeinv:";
