-- Self-grantable roles plugin removed entirely — role_panels already covers the same job
-- (button/reaction role claiming) with a full dashboard editor, embeds, and more trigger types.
DROP TABLE IF EXISTS self_role_panels;
